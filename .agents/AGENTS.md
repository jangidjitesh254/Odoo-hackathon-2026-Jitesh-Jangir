# Git Commit Guidelines

Always provide a suggested git commit message at the end of each response containing code modifications. Use conventional commit structure (e.g. `feat(component): description`).
